__version__ = "0.9.3 TEST PRODUCTION"
__production__ = True # Used to indicate if this is a production build. Can be changed post-build to subscribe to a different channel during update checks.
# These don't go into .env as GitHub Actions needs to be able to access them.